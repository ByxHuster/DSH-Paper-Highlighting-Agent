# 领域地图（field-map）

> 领域发展线上下文（设计 §7）。Agent 判断语句「分量」时按需注入：global-read 通读时必读，
> propose 遇到范式转移节点/领域定位判断时参考；**不常驻 system prompt**。
> 由 Agent 在论文级反思收尾时提议增补（v0.4 Phase 3+），用户可手工编辑。
> 维护约定：只追加、标注来源论文与日期、不删改既有条目（与 reflections append-only 同精神）。

## 1. 领域主线（NLP 深度学习，2013–2016）

```
分布式词表示（word2vec 2013）
  → 序列端到端建模（seq2seq 2014）
  → 注意力机制解决信息瓶颈（attention 2015–2016）
  → [空白区] Transformer / 预训练 / LLM（Vaswani 2017 / ELMo·BERT 2018 / GPT 系）
```

主线一句话：从「把词变成向量」（静态分布式表示）到「把任意变长序列映射为序列」（端到端
encoder-decoder），再到「用注意力在解码时动态对齐源端」（固定向量信息瓶颈的解法），最终
收敛到「自注意力 + 大规模预训练」的当代范式。本工作区已收录的三篇论文恰好是这条主线的
三个节点。

## 2. 已收录论文定位

### 2.1 Mikolov 2013 — Word2Vec（词向量）
- **贡献**：高效词向量训练（CBOW / Skip-gram + 负采样/层次 softmax），把分布假设工程化为可规模化、可迁移的词表示。
- **继承**：Bengio 2003 NNLM（前馈语言模型）、Mikolov 2010 RNNLM、Collobert & Weston 2008（C&W 窗口打分）。
- **引领**：GloVe / fastText / ELMo / BERT 的词与子词表示基础；「表示学习」成为后续 NLP 默认前提。
- **范式地位**：分布式表示（distributional semantics）范式的工程化里程碑。
- **检索标记**：`#word2vec` `#词向量` `#分布式表示` `#2013`

### 2.2 Sutskever 2014 — Seq2Seq（序列到序列）
- **贡献**：两个 LSTM（encoder→decoder）端到端学习变长序列映射——「编码器读入全部输入、解码器生成输出」。
- **继承**：RNN/LSTM（Hochreiter & Schmidhuber 1997）、同期 Cho et al. 2014 的 encoder-decoder。
- **引领**：神经机器翻译 / 对话 / 摘要等一切 seq2seq 任务；是 attention 的直接前提。
- **范式地位**：端到端学习范式；同时暴露「固定长度向量瓶颈」这一关键问题。
- **检索标记**：`#seq2seq` `#encoder-decoder` `#端到端` `#2014`

### 2.3 Bahdanau 2016 — Attention（注意力 / 联合对齐）
- **贡献**：软注意力机制——解码每步动态对齐源端片段，联合学习对齐与翻译，破解长序列固定向量瓶颈。
- **继承**：Seq2Seq（Sutskever 2014）、Cho et al. 2014。
- **引领**：Transformer self-attention（Vaswani 2017）的直接思想前提；对齐/归因可解释性研究。
- **范式地位**：注意力范式的奠基——从「压缩到固定向量」转向「解码期动态读取源端」。
- **检索标记**：`#attention` `#对齐` `#NMT` `#2016`

## 3. 里程碑 / 范式转移标注

| 节点 | 年份 | 范式转移内容 | 对应论文 |
|---|---|---|---|
| 分布式词表示 | 2013 | 词义 → 低维稠密向量（可迁移特征） | Mikolov 2013 |
| 端到端序列映射 | 2014 | 手工特征管线 → encoder-decoder 联合学习 | Sutskever 2014 |
| 注意力对齐 | 2015–2016 | 固定编码向量 → 解码期动态对齐源端 | Bahdanau 2016 |
| （空白）自注意力/预训练 | 2017+ | 全注意力 + 大规模预训练 → LLM | Vaswani 2017 / BERT / GPT |

> **判断语句「分量」的用法**：某句若**首次提出 / 首次规模化**某一范式节点，即便措辞平淡也应
> 高亮为「核心洞见/贡献」类（red）；若只是复述既有范式或常规工程细节，则按该节密度基线处理，
> 不过度高亮。

## 4. 空白区（待后续论文累积）

- 2017 Transformer（self-attention、positional encoding）——目前仅作主线标注，**尚未收录论文**。
- 2018 语境化/预训练表示（ELMo / BERT）与 2018+ GPT 系（自回归预训练 → LLM）。
- RNN 变体（GRU）、束搜索 / BLEU 等支撑性技术节点。
- 跨领域迁移（多模态、Agent 化）——若收录此类论文再扩线。
- **增补约定**：收录新论文时在 §2 追加小节 + 若构成新范式节点在 §3 里程碑表加行，标注来源论文与日期（append-only）。
